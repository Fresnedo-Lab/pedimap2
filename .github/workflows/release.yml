# .github/workflows/release.yml
# ================================
# Triggered on every push of a version tag (v*.*.*).
# Builds native installers for all three platforms in parallel,
# then creates a GitHub Release and uploads the artefacts.
#
# Matrix:
#   windows-latest  →  .msi  +  .exe  (NSIS)
#   macos-latest    →  .dmg  (universal binary via lipo is a roadmap item;
#                             currently builds for the runner arch, arm64 on GH)
#   ubuntu-22.04    →  .AppImage  +  .deb
#
# Required secrets:
#   (none required for unsigned builds)
#
#   Optional codesigning secrets (set in repo Settings → Secrets):
#   APPLE_CERTIFICATE          base64-encoded .p12 certificate
#   APPLE_CERTIFICATE_PASSWORD password for the .p12
#   APPLE_SIGNING_IDENTITY     Developer ID Application: ...
#   APPLE_ID                   notarisation Apple ID e-mail
#   APPLE_PASSWORD             app-specific password for notarisation
#   APPLE_TEAM_ID              10-char Apple team ID
#   WINDOWS_CERTIFICATE        base64-encoded .pfx
#   WINDOWS_CERTIFICATE_PASSWORD password for the .pfx

name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            rust_target: x86_64-pc-windows-msvc
            python_arch: x64

          - platform: macos-latest
            rust_target: aarch64-apple-darwin
            python_arch: arm64

          - platform: macos-13
            rust_target: x86_64-apple-darwin
            python_arch: x64

          - platform: ubuntu-22.04
            rust_target: x86_64-unknown-linux-gnu
            python_arch: x64

    runs-on: ${{ matrix.platform }}
    name: Build – ${{ matrix.rust_target }}

    steps:
      # ── Checkout ────────────────────────────────────────────────────────────
      - uses: actions/checkout@v4

      # ── Rust toolchain ───────────────────────────────────────────────────────
      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      # ── Node.js / frontend ──────────────────────────────────────────────────
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Build React frontend
        run: npm run build
        working-directory: frontend

      # ── Python + PyInstaller sidecar ────────────────────────────────────────
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          architecture: ${{ matrix.python_arch }}
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install Python dependencies
        run: pip install -r backend/requirements.txt pyinstaller

      - name: Build Python sidecar (PyInstaller)
        run: pyinstaller pedimap_backend.spec --distpath dist --workpath build --noconfirm --clean
        working-directory: backend

      # ── Place sidecar binary with Tauri target-triple suffix ─────────────────
      - name: Stage sidecar binary (Unix)
        if: matrix.platform != 'windows-latest'
        run: |
          mkdir -p src-tauri/binaries
          cp backend/dist/pedimap-backend \
             "src-tauri/binaries/pedimap-backend-${{ matrix.rust_target }}"
          chmod +x "src-tauri/binaries/pedimap-backend-${{ matrix.rust_target }}"

      - name: Stage sidecar binary (Windows)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force src-tauri/binaries | Out-Null
          Copy-Item backend\dist\pedimap-backend.exe `
            "src-tauri\binaries\pedimap-backend-${{ matrix.rust_target }}.exe"

      # ── Linux system libraries (WebKit2GTK) ─────────────────────────────────
      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.0-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libgtk-3-dev

      # ── macOS codesigning (optional) ────────────────────────────────────────
      - name: Import macOS certificate
        if: |
          startsWith(matrix.platform, 'macos') &&
          env.APPLE_CERTIFICATE != ''
        env:
          APPLE_CERTIFICATE:          ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          echo "$APPLE_CERTIFICATE" | base64 --decode > certificate.p12
          security create-keychain -p "" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security import certificate.p12 \
            -k build.keychain \
            -P "$APPLE_CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign
          security set-key-partition-list \
            -S apple-tool:,apple: -s \
            -k "" build.keychain

      # ── Build Tauri app ─────────────────────────────────────────────────────
      - name: Build Tauri application
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN:              ${{ secrets.GITHUB_TOKEN }}
          TAURI_PRIVATE_KEY:         ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_KEY_PASSWORD:        ${{ secrets.TAURI_KEY_PASSWORD }}
          # macOS notarisation (only used if secrets are set):
          APPLE_SIGNING_IDENTITY:    ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID:                  ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD:            ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID:             ${{ secrets.APPLE_TEAM_ID }}
          # Windows codesigning (only used if secrets are set):
          WINDOWS_CERTIFICATE:       ${{ secrets.WINDOWS_CERTIFICATE }}
          WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        with:
          projectPath:   .
          tauriScript:   cargo tauri
          tagName:       ${{ github.ref_name }}
          releaseName:   "Pedimap 2 ${{ github.ref_name }}"
          releaseBody:   |
            ## Pedimap 2 ${{ github.ref_name }}

            ### Downloads
            | Platform | File |
            |---|---|
            | Windows 10/11 (x64) | `Pedimap2_*_x64-setup.exe` or `Pedimap2_*_x64_en-US.msi` |
            | macOS Apple Silicon  | `Pedimap2_*_aarch64.dmg` |
            | macOS Intel          | `Pedimap2_*_x64.dmg` |
            | Linux x86_64        | `pedimap2_*_amd64.AppImage` or `pedimap2_*_amd64.deb` |

            > **Note:** On macOS, right-click → Open on first launch to bypass Gatekeeper (unsigned builds).
            > On Linux, mark the AppImage executable: `chmod +x pedimap2_*.AppImage`
          releaseDraft:  true
          prerelease:    false
          args:          --target ${{ matrix.rust_target }}
