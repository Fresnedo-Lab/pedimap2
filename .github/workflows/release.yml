# .github/workflows/release.yml
# ================================
# Triggered on every push of a version tag (v*.*.*).
# Builds native installers for all platforms in parallel,
# then creates a GitHub Release draft and uploads the artifacts.
#
# Matrix:
#   windows-latest   →  .msi  +  .exe  (NSIS)
#   macos-latest     →  .dmg  aarch64  (Apple Silicon, native)
#   macos-latest     →  .dmg  x86_64   (Intel, Rust cross-compile; sidecar runs via Rosetta 2)
#   ubuntu-22.04     →  .AppImage  +  .deb
#
# Required secrets: none (for unsigned builds)
#
# Optional codesigning secrets (Settings → Secrets → Actions):
#   APPLE_CERTIFICATE            base64-encoded .p12 certificate
#   APPLE_CERTIFICATE_PASSWORD   password for the .p12
#   APPLE_SIGNING_IDENTITY       Developer ID Application: ...
#   APPLE_ID                     notarization Apple ID email
#   APPLE_PASSWORD               app-specific password for notarization
#   APPLE_TEAM_ID                10-char team ID
#   WINDOWS_CERTIFICATE          base64-encoded .pfx
#   WINDOWS_CERTIFICATE_PASSWORD password for the .pfx

name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # ── Windows x86_64 ─────────────────────────────────────────────────
          - platform: windows-latest
            rust_target: x86_64-pc-windows-msvc
            python_arch: x64

          # ── macOS arm64 (Apple Silicon — native build) ─────────────────────
          - platform: macos-latest
            rust_target: aarch64-apple-darwin
            python_arch: arm64

          # ── macOS x86_64 (Intel — Rust cross-compile on arm64 runner) ──────
          # PyInstaller sidecar is arm64; runs transparently under Rosetta 2.
          - platform: macos-latest
            rust_target: x86_64-apple-darwin
            python_arch: arm64

          # ── Linux x86_64 ───────────────────────────────────────────────────
          - platform: ubuntu-22.04
            rust_target: x86_64-unknown-linux-gnu
            python_arch: x64

    runs-on: ${{ matrix.platform }}
    name: Build – ${{ matrix.rust_target }}

    steps:
      - uses: actions/checkout@v4

      # ── Rust toolchain ────────────────────────────────────────────────────
      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Add x86_64 Rust target for macOS cross-compile
        if: matrix.rust_target == 'x86_64-apple-darwin'
        run: rustup target add x86_64-apple-darwin

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      # ── Node.js / frontend ────────────────────────────────────────────────
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Build React frontend
        run: npm run build
        working-directory: frontend

      # ── Python + PyInstaller sidecar ──────────────────────────────────────
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          architecture: ${{ matrix.python_arch }}
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install Python dependencies
        run: pip install -r backend/requirements.txt pyinstaller

      - name: Build Python sidecar (PyInstaller)
        run: pyinstaller pedimap_backend.spec --distpath dist --workpath build --noconfirm --clean
        working-directory: backend

      # ── Stage sidecar binary ──────────────────────────────────────────────
      - name: Stage sidecar binary (Unix)
        if: matrix.platform != 'windows-latest'
        run: |
          mkdir -p src-tauri/binaries
          cp backend/dist/pedimap-backend \
             "src-tauri/binaries/pedimap-backend-${{ matrix.rust_target }}"
          chmod +x "src-tauri/binaries/pedimap-backend-${{ matrix.rust_target }}"

      - name: Stage sidecar binary (Windows)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force src-tauri/binaries | Out-Null
          Copy-Item backend\dist\pedimap-backend.exe `
            "src-tauri\binaries\pedimap-backend-${{ matrix.rust_target }}.exe"

      # ── Linux system libraries ────────────────────────────────────────────
      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.0-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libgtk-3-dev

      # ── macOS codesigning (skipped when secrets are absent) ───────────────
      # FIX: secrets context must be inside ${{ }} — bare `if:` block scalars
      # (using |) do not evaluate the secrets context, causing the workflow
      # parser to reject the file with "Unrecognized named-value: 'secrets'".
      - name: Import macOS certificate
        if: ${{ matrix.platform == 'macos-latest' && secrets.APPLE_CERTIFICATE != '' }}
        env:
          APPLE_CERTIFICATE:          ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD:          ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          KEYCHAIN_PATH=$RUNNER_TEMP/signing.keychain-db
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          echo "$APPLE_CERTIFICATE" | base64 --decode > /tmp/certificate.p12
          security import /tmp/certificate.p12 \
            -k "$KEYCHAIN_PATH" \
            -P "$APPLE_CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign
          security list-keychain -d user -s "$KEYCHAIN_PATH"

      # ── Tauri build ───────────────────────────────────────────────────────
      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN:                    ${{ secrets.GITHUB_TOKEN }}
          APPLE_CERTIFICATE:               ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD:      ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY:          ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID:                        ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD:                  ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID:                   ${{ secrets.APPLE_TEAM_ID }}
          WINDOWS_CERTIFICATE:             ${{ secrets.WINDOWS_CERTIFICATE }}
          WINDOWS_CERTIFICATE_PASSWORD:    ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Pedimap 2 ${{ github.ref_name }}'
          releaseBody: |
            ## What's new
            See [CHANGELOG.md](https://github.com/Fresnedo-Lab/pedimap2/blob/main/CHANGELOG.md)
            for the full change history.

            ### Platform installers
            | Platform | File |
            |----------|------|
            | Windows  | `.msi` or `.exe` |
            | macOS (Apple Silicon) | `.dmg` (aarch64) |
            | macOS (Intel — via Rosetta 2) | `.dmg` (x86_64) |
            | Linux | `.AppImage` / `.deb` |
          releaseDraft: true
          prerelease: false
          args: --target ${{ matrix.rust_target }}
